---
  apiVersion: v1
  kind: Pod
  metadata:
    name: spark12
    #namespace: spark-and-postgres
    labels:
      app: spark12
  spec:
    containers:
      - name: spark12
        image: datamechanics/spark
        command: ["bash", "-c", "while true; do echo 1; sleep 1; done"]
        ports:
          - containerPort: 7077
          - containerPort: 8080
      #- name: kube-proxy-sidecar
        #image: bitnami/kubectl
        #command: ["kubectl", "proxy"]
        #ports:
          #- containerPort: 8001
          